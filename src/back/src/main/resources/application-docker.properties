# Perfil usado pelo container (docker compose). Evita loop de reinício do DevTools
# ao compilar em target/ com o código montado em /app.
spring.devtools.restart.enabled=false
spring.devtools.livereload.enabled=false

# Aguarda o banco subir sem falhar a aplicação imediatamente
spring.datasource.hikari.initialization-fail-timeout=60000

jwt.secret=${JWT_SECRET:ctrlfleet-dev-secret-key-min-32-chars!!}

# Horários de reserva/checklist são em Brasília (mesmo valor gravado no banco).
spring.jackson.time-zone=America/Sao_Paulo

spring.mail.host=${SPRING_MAIL_HOST:mailhog}
spring.mail.port=${SPRING_MAIL_PORT:1025}
spring.mail.username=${SPRING_MAIL_USERNAME:}
spring.mail.password=${SPRING_MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=${SPRING_MAIL_SMTP_AUTH:false}
spring.mail.properties.mail.smtp.starttls.enable=${SPRING_MAIL_SMTP_STARTTLS:false}

ctrlfleet.mail.enabled=${CTRLFLEET_MAIL_ENABLED:true}
ctrlfleet.mail.from=${CTRLFLEET_MAIL_FROM:noreply@ctrlfleet.gov.br}
ctrlfleet.mail.from-name=${CTRLFLEET_MAIL_FROM_NAME:CtrlFleet}
ctrlfleet.mail.app-url=${CTRLFLEET_MAIL_APP_URL:}
